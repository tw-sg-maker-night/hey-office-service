{
  "name": "hey-office-service",
  "version": "1.0.0",
  "description": "ThoughtWorks Singapore office butler",
  "main": "src/server.js",
  "scripts": {
    "build": "babel src -d build",
    "start": "node build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "babel-preset-node6": "^11.0.0",
    "express": "^4.14.0"
  },
  "babel": {
    "presets": ["node6"]
  }
}
